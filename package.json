{
  "name": "telbot-ticket",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --use-env-proxy src/server.js",
    "test": "vitest run",
    "dev": "node --env-file=.env --watch --use-env-proxy src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18"
  }
}
